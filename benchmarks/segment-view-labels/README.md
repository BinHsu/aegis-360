# Segment-view benchmark labels

These configs record agent review of exact three-sample, four-cardinal story
segment packets. They are benchmark evidence, not owner ground truth and not a
product-time requirement. `abstain` is intentional when no one candidate is a
stable primary across the segment.
